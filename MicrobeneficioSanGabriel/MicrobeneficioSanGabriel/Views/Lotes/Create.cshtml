@model MicrobeneficioSanGabriel.Models.Lote

@{
    ViewData["Title"] = "Nuevo lote";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/VistaCrear.css" />
}

<div class="VistaCreaR-top">
    <div class="VistaCreaR-title">
        <span class="VistaCreaR-badge">
            <i class="fa-solid fa-layer-group"></i> Recepción de lotes
        </span>
        <h1>Nuevo lote</h1>
        <p>
            Seleccione una finca; el productor y el código se asignarán de forma automática.
        </p>
    </div>
</div>

<div class="sg-card">
    <form asp-action="Create" method="post">
        <partial name="_Form" model="Model" />

        <div class="d-flex justify-content-end gap-2 mt-4">
            <a asp-action="Index" class="btn btn-outline-secondary btn-cancelar">
                <i class="fa-solid fa-arrow-left me-1"></i>Cancelar
            </a>
            <button type="submit" class="btn btn-success btn-crear">
                <i class="fa-solid fa-floppy-disk me-2"></i>Registrar lote
            </button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}